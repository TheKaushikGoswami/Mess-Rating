const breakfast_stars = document.querySelectorAll('.rating-breakfast');

stars.forEach(star => {
  star.addEventListener('change', function () {
    displayRating.textContent = this.value;
  });
});
const lunch_stars = document.querySelectorAll('.rating');

stars.forEach(star => {
  star.addEventListener('change', function () {
    displayRating.textContent = this.value;
  });
});
const stars = document.querySelectorAll('.rating');

stars.forEach(star => {
  star.addEventListener('change', function () {
    displayRating.textContent = this.value;
  });
});
const stars = document.querySelectorAll('.rating');

stars.forEach(star => {
  star.addEventListener('change', function () {
    displayRating.textContent = this.value;
  });
});
const stars = document.querySelectorAll('.rating');
const displayRating = document.getElementById('displayRating');

stars.forEach(star => {
  star.addEventListener('change', function () {
    displayRating.textContent = this.value;
  });
});